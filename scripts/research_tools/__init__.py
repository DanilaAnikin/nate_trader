"""Research instruments. Deliberately NOT named `research`.

`scripts/research.py` already exists and is a strategy-identity source, so a
package of that name shadows it and breaks every import of the real module.
"""
