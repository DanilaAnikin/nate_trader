import SignalsClient from "@/components/SignalsClient";

export const metadata = { title: "Signals & universe · V11 Adaptive Momentum" };

export default function SignalsPage() {
  return <SignalsClient />;
}
