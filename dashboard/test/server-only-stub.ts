// Test stub for the `server-only` package. The real module throws unless it
// is bundled with React Server Components; under vitest we alias it here so
// server-only modules can be unit-tested directly.
export {};
