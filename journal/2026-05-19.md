# Trading Journal — 2026-05-19

## Major refactor: ALPHA_PLAN.md — path to +5–10%/yr OOS alpha

Implemented Phases A–J of the alpha-improvement plan in a single sweep
of 10 commits. The goal was recalibrated from the unrealistic
"+5%/month" to a measurable "+5–10%/yr walk-forward OOS alpha." See
`ALPHA_PLAN.md` for full diagnosis and rationale.

### Shipped in this batch

| Phase | Commit | Change |
|-------|--------|--------|
| A | `a7dada0` | Module-ablation env flags (ML, PEAD, mean-rev, sentiment, multi-tf, earnings, sector-rot) |
| B | `bd07b40` | `sweep --metric=wf_alpha --holdout-start=…` makes WF OOS alpha the optimization metric |
| C | `fad0d7c` | Sector-relative RS replaces SPY-relative in scoring + gate |
| D | `8637e1f` | Optional vol-targeted position sizing (`target_vol_per_position_pct`) |
| E | `d267b00` | Wired `gap_scanner.score_gap_from_technicals` into scoring pipeline |
| F | `82f8df9` | Live-vs-backtest drift monitor + EOD wiring |
| G | `78faf5a` | Heartbeat workflow — opens infra issue if journal missing |
| H | `d1dba70` | Regime-adaptive universe liquidity filter |
| I | `ec5b2aa` | CLAUDE.md + sp500_benchmark.md goal recalibrated |
| J | (this commit) | `scripts/run_ablation_study.py` runner |

### What still needs to happen (Phase J execution)

The code is in place but the actual ablation backtests have **not yet
been run** — this sandbox lacks pandas + Alpaca network access. To
execute:

```bash
python3 scripts/run_ablation_study.py \
  --start 2022-01-01 \
  --holdout-start 2025-01-01 \
  --walk-forward-best
```

This runs 8 backtests (baseline + one per ablated module), ranks them
by alpha, and identifies modules whose removal *raises* alpha. Result
lands at `state/backtest/ablation_study.json`. Run from CI (the
auto-iteration workflow already has pandas + Alpaca secrets).

After Phase J completes, the next iteration should drop the
net-negative modules from production, re-run a WF sweep with
`--metric=wf_alpha`, and ship the surviving config as v10.

### Risk register (live)

- The shipping criterion is now WF OOS ≥ +5%/yr. The latest WF run
  (2026-05-17, `walk_forward_20260517_110815`) had mean OOS alpha
  −7.4%/yr. We are **far** from the ship criterion; expect 2–3 more
  iterations after ablation before v10 stabilizes.
- All Phase A–H changes default to **legacy behavior** unless explicitly
  enabled (vol-targeted sizing, sector-relative RS uses sector data
  only when available, ablation flags off-by-default, holdout-start
  optional). v7 remains the production config until a backtest validates
  the new pieces.

## Market Conditions

(End-of-day routine hasn't run yet for 2026-05-19; placeholders.)

- SPY: see `state/spy_history.json`
- VIX: not tracked yet
- Risk tier (live): CAUTIOUS (from drawdown rule, not from drift monitor — drift monitor has no baseline until first WF run after this batch)

## Reflection

- What worked: shipping the plan as 10 small commits keeps every phase
  bisect-friendly. If v10 regresses, we know exactly which phase to
  isolate.
- What didn't: I can't run backtests from this dev environment. Phase
  J is wired but not measured. The user (or CI) needs to execute and
  paste back the result before we can claim alpha actually improved.
- Tomorrow's plan: run `scripts/run_ablation_study.py` in CI, post the
  result in `state/backtest/ablation_study.json`, then prune the
  net-negative modules in a Phase-K commit.
